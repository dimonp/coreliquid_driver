[Unit]
Description=MSI MEG CoreLiquid S360 Daemon

[Service]
Type=simple
ExecStart=/home/ben/src/coreliquid_driver/src/my_msi_driver -M 0 startd
ExecReload=/bin/kill -HUP $MAINPID
ExecStop=/bin/kill -TERM $MAINPID
KillMode=mixed
Restart=on-failure

[Install]
WantedBy=multi-user.target
